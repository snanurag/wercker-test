version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.1-jessie
    steps:
      - checkout
      - run: echo "Hello from build"
  test:
    docker:
      - image: circleci/ruby:2.4.1-jessie
    steps:
      - checkout
      - run: echo "Hello from test"
workflows:
  version: 2
  tagged-build:
    jobs:
      - build:
          filters:
            tags:
              only: 2.0
